---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-fos-default-ns
  namespace: default
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: fos
        - namespaceSelector:
            matchLabels:
              name: default
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              name: fos
        - namespaceSelector:
            matchLabels:
              name: default
